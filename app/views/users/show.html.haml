.yui3-g
  .yui3-u-1
    %h2= @user
.yui3-g
  .yui3-u-1
    .gradient_border_wrap
      .gradient_border_content
        %h3= @user.email
        - if @user == current_user
          = link_to 'Change Password', edit_user_path(@user)
        %h3
          - if @user.locked_at?
            User is locked.
        = render :partial => 'shared/calendar', :locals => { :start_date => Date.today.beginning_of_week, :user => @user }
        = render :partial => 'shared/calendar', :locals => { :start_date => Date.today.beginning_of_week - 1.week, :user => @user }
